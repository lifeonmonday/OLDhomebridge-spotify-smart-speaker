{
  "pluginAlias": "SpotifySmartSpeaker",
  "pluginType": "platform",
  "singular": true,
  "headerDisplay": "Configure your Spotify API credentials and target device.",
  "schema": {
    "type": "object",
    "properties": { "name": {
        "title": "Platform Name",
        "type": "string",
        "default": "SpotifySmartSpeaker",
        "required": true
      },
      "clientId": {
        "title": "Spotify Client ID",
        "type": "string",
        "required": true
      },
      "clientSecret": {
        "title": "Spotify Client Secret",
        "type": "string",
        "required": true
      },
      "refreshToken": {
        "title": "Spotify Refresh Token",
        "type": "string",
        "required": true
      },
      "deviceId": {
        "title": "Spotify Target Device ID",
        "type": "string",
        "description": "The Spotify Connect ID for your target speaker or Nest group.",
        "required": true
      }
    }
  }
}
